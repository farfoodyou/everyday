#include <cstdio>
#include <algorithm>

int main(){

    int n, m;
    scanf("%d",&n);
    while (n--) {
        int a, b;
        scanf("%d%d",&a, &b);
        printf("%d\n", b*2);
    }

    return 0;
}